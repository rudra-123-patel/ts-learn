"use strict";
// union types (OR)
function setStatus(status) {
    console.log(`Status set to ${status}`);
}
setStatus("rejected"); // Valid
setStatus("approved"); // Valid
setStatus("pending"); // Valid
let myCicrcle = {
    color: "red",
    radius: 10,
};
//# sourceMappingURL=06-union-interaction.js.map